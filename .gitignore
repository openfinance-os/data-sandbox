node_modules/
.DS_Store
.env
.env.local
*.log
npm-debug.log*
.vitest-cache/
playwright-report/
test-results/
# Visual baselines are pinned to Linux (CI parity). Ignore platform-specific
# baselines generated by local macOS / Windows runs so they can't be
# accidentally committed — only `*-linux.png` belongs in the repo.
tests/e2e/**/*-darwin.png
tests/e2e/**/*-win32.png
.lighthouseci/
coverage/
_site/
# Generated fixture-package contents (data/, bundles/, etc.). The package
# scaffolding (package.json / pyproject.toml / index.* / READMEs) is
# checked in; the data is regenerated by `npm run build:fixtures`.
packages/sandbox-fixtures/bundles/
packages/sandbox-fixtures/personas/
packages/sandbox-fixtures/enrichment/
packages/sandbox-fixtures/brands/
packages/sandbox-fixtures/brand-registry.json
packages/sandbox-fixtures/manifest.json
packages/sandbox-fixtures/spec.json
packages/sandbox-fixtures/spec.insurance.json
packages/sandbox-fixtures/spec.atm.json
packages/sandbox-fixtures/pools.json
packages/sandbox-fixtures/lib/
# Build-cache stamp (content hash of the corpus inputs) — local-only.
packages/sandbox-fixtures/.build-stamp
packages/sandbox-fixtures-py/src/openfinance_os_sandbox_fixtures/data/
packages/sandbox-fixtures-py/dist/
packages/sandbox-fixtures-py/build/
*.egg-info/
__pycache__/
